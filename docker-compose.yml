version: '3.8'

services:
  astro-prompt-admin:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: astro-prompt-admin
